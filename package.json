{
  "name": "ai-tutor",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @tutor/api dev",
    "dev:web": "pnpm --filter @tutor/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
